# Réglages pnpm (pnpm 11 ne lit plus le champ "pnpm" de package.json).
# Aucun script de build de dépendance ne doit s'exécuter à l'installation.
# ignoreScripts (clé settings) supprime aussi l'échec ERR_PNPM_IGNORED_BUILDS,
# contrairement à ignore-scripts du .npmrc.
ignoreScripts: true
